import React from "react";
import "./Hero.css"

function HeroComponent() {
    return (
        <div className="hero-wrapper">
            <h1 className="hero-text">Добре дошли в Моята библиотека!</h1>
        </div>
    );
};

export default HeroComponent;